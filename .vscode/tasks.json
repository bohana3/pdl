{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build solution",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "${workspaceRoot}/cmd/pdl/main.go"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}